#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../fire/bin/firejs" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../fire/bin/firejs" "$@"
  ret=$?
fi
exit $ret
